"""open-video scripts (data package).

Exists so install.sh (the resumable weight installer that `open-video pull`
delegates to) and the H3 download manifest ship inside the wheel as
open_video.scripts package data. The .py files here are standalone dev/agent
tools run as scripts, not imported as modules.
"""
