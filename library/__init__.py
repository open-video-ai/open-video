"""open-video prompt library (data package).

Exists so the prompt presets under prompts/ ship inside the wheel as
open_video.library package data; the CLI's list-presets resolves
REPO_ROOT/library/prompts both in a source checkout and when installed.
"""
