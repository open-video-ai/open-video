# No runtime deps — stdlib only (authoritative: pyproject.toml dependencies = []).
# Kept because scripts/install.sh and docs reference `pip install -r requirements.txt`
# as a fallback; installing this file is a no-op by design.
